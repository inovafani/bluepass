export default function InternalAppPage() {
  return (
    <section className="mx-auto max-w-5xl px-5 py-16">
      <p className="text-sm font-semibold tracking-[0.18em] text-bluepass-ocean">
        Internal app
      </p>
      <h1 className="bp-page-title mt-4 text-[clamp(2.25rem,5vw,4rem)]">
        BluePass operations workspace
      </h1>
      <p className="mt-6 text-lg leading-8 text-slate-700">
        Future dashboards for support, booking review, integration health, and
        transfer reconciliation will live here.
      </p>
    </section>
  );
}
